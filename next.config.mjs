/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // Turbopack is enabled by passing --turbo to next dev, no need to configure it here.
  experimental: {
    // any experimental features can go here
  },
};

export default nextConfig;
